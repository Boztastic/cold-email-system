╔═══════════════════════════════════════════════════════════════════════════════╗
║                                                                               ║
║   🔒 COLD EMAIL SYSTEM v3.1 - SECURE VERSION DEPLOYMENT                      ║
║                                                                               ║
║   Full Security Package: Encryption, User Isolation, Validation              ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════════════════════
🛡️  WHAT'S INCLUDED IN THE SECURITY PACKAGE
═══════════════════════════════════════════════════════════════════════════════

✅ AES-256-GCM ENCRYPTION
   • SMTP passwords encrypted before storage
   • Cloudflare API tokens encrypted before storage
   • Encryption key stored as environment variable
   • Decryption only happens at runtime

✅ USER ISOLATION
   • Each user only sees their own data
   • Contacts, templates, campaigns, sequences all user-scoped
   • Analytics and stats per-user
   • No cross-user data leakage

✅ OWNERSHIP VERIFICATION
   • Every edit/delete checks resource ownership
   • Middleware verifies before action executes
   • Audit logging on access violations

✅ INPUT VALIDATION
   • Email format validation
   • Domain name validation
   • URL validation
   • Max length enforcement
   • UUID format validation

✅ INPUT SANITIZATION
   • HTML sanitized to prevent XSS
   • Script tags removed from email bodies
   • Event handlers stripped
   • Subject line injection prevented

✅ BRUTE FORCE PROTECTION
   • 5 failed attempts = 15 minute lockout
   • Per IP + email combination tracking
   • Automatic cleanup of old entries

✅ PASSWORD REQUIREMENTS
   • Minimum 8 characters
   • At least one uppercase letter
   • At least one lowercase letter
   • At least one number
   • At least one special character

✅ RATE LIMITING
   • Global: 200 requests per 15 minutes
   • Auth endpoints: 20 requests per 15 minutes
   • Per-user: 100 requests per minute

✅ AUDIT LOGGING
   • Login attempts (success/failure)
   • Password changes
   • User invitations
   • Resource access violations
   • Campaign/sequence starts


═══════════════════════════════════════════════════════════════════════════════
📥 DOWNLOAD THESE 4 FILES
═══════════════════════════════════════════════════════════════════════════════

Save in your cold-email-system/ folder:

┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  1. server-secure.js     →  Save as: server.js                              │
│  2. database-secure.js   →  Save as: database.js                            │
│  3. security.js          →  Save as: security.js (NEW FILE)                 │
│  4. package-v3.json      →  Save as: package.json                           │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend remains the same - use App-v3-complete.jsx


═══════════════════════════════════════════════════════════════════════════════
🔑 GENERATE SECURITY KEYS (CRITICAL!)
═══════════════════════════════════════════════════════════════════════════════

Before deploying, generate secure keys. Run these in your terminal:

┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  # Generate JWT Secret (64 bytes hex = 128 characters)                       │
│  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"   │
│                                                                              │
│  # Generate Encryption Key (32 bytes hex = 64 characters)                    │
│  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

⚠️  SAVE THESE KEYS SECURELY! If you lose the encryption key, you cannot
    decrypt stored SMTP passwords or API tokens!


═══════════════════════════════════════════════════════════════════════════════
⚙️  ENVIRONMENT VARIABLES (Render Dashboard)
═══════════════════════════════════════════════════════════════════════════════

Go to Render → Your Backend Service → Environment

Add/Update these variables:

┌──────────────────────────────────────────────────────────────────────────────┐
│  Variable Name     │  Value                                                 │
├────────────────────┼────────────────────────────────────────────────────────┤
│  DATABASE_URL      │  (auto-set by Render PostgreSQL)                       │
│                    │                                                        │
│  JWT_SECRET        │  [paste 128-char hex from step above]                  │
│                    │                                                        │
│  ENCRYPTION_KEY    │  [paste 64-char hex from step above]                   │
│                    │                                                        │
│  ADMIN_EMAIL       │  admin@yourdomain.com                                  │
│                    │                                                        │
│  ADMIN_PASSWORD    │  [Strong password with upper, lower, number, symbol]   │
│                    │                                                        │
│  FRONTEND_URL      │  https://cold-email-frontend-6xf7.onrender.com         │
│                    │                                                        │
│  BACKEND_URL       │  https://cold-email-system-1.onrender.com              │
│                    │                                                        │
│  NODE_ENV          │  production                                            │
└──────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
🚀 DEPLOYMENT STEPS
═══════════════════════════════════════════════════════════════════════════════

STEP 1: Update Environment Variables on Render
─────────────────────────────────────────────────
1. Go to Render Dashboard
2. Select your backend service
3. Go to Environment tab
4. Add JWT_SECRET and ENCRYPTION_KEY (generated above)
5. Verify other variables are set


STEP 2: Update Backend Code
─────────────────────────────────────────────────
1. Download the 4 files listed above
2. Replace in cold-email-system/:
   • server.js (with server-secure.js)
   • database.js (with database-secure.js)
   • package.json (with package-v3.json)
3. Add new file:
   • security.js

4. Deploy:
   ┌────────────────────────────────────────────────────────────────────────┐
   │  cd cold-email-system                                                  │
   │  git add .                                                             │
   │  git commit -m "Add security package v3.1"                             │
   │  git push                                                              │
   └────────────────────────────────────────────────────────────────────────┘


STEP 3: Verify Deployment
─────────────────────────────────────────────────
1. Wait for Render to deploy (5-10 minutes)
2. Check logs for:
   ┌────────────────────────────────────────────────────────────────────────┐
   │  🔒 Cold Email System v3.1 - SECURE VERSION                            │
   │  ✅ Database connected successfully                                    │
   │  📧 Starting email processor...                                        │
   └────────────────────────────────────────────────────────────────────────┘

3. If you see these warnings, SET THE VARIABLES:
   • "⚠️ Set JWT_SECRET environment variable!"
   • "⚠️ Set ENCRYPTION_KEY environment variable!"


STEP 4: Re-add Sensitive Data
─────────────────────────────────────────────────
⚠️  IMPORTANT: After deploying with encryption, you need to re-add:
    • SMTP accounts (passwords will now be encrypted)
    • Cloudflare API token (will now be encrypted)

Old data stored in plain text won't work with the new encrypted system.

1. Go to Warming page → Add your SMTP accounts again
2. Go to Domains page → Reconnect Cloudflare


═══════════════════════════════════════════════════════════════════════════════
⚠️  IMPORTANT NOTES
═══════════════════════════════════════════════════════════════════════════════

ENCRYPTION KEY BACKUP
─────────────────────
• Store your ENCRYPTION_KEY somewhere safe (password manager)
• If you lose it, you cannot decrypt stored passwords
• Never commit it to Git

JWT SECRET
─────────────────────
• If you change JWT_SECRET, all users will be logged out
• This is by design - it invalidates all existing tokens

PASSWORD REQUIREMENTS
─────────────────────
• New users must meet password requirements
• Admin password should be changed to meet requirements
• Example: "MySecure@Pass123"

EXISTING DATA
─────────────────────
• Existing plain-text passwords won't work after upgrade
• You'll need to re-enter SMTP credentials
• This is a security feature, not a bug

DATABASE MIGRATION
─────────────────────
• New user_id columns are added automatically
• Existing data will need user_id assignment
• For fresh deployments, this isn't an issue


═══════════════════════════════════════════════════════════════════════════════
🧪 TESTING THE SECURITY
═══════════════════════════════════════════════════════════════════════════════

After deployment, test these security features:

1. BRUTE FORCE PROTECTION
   • Try 5 wrong passwords → Should see lockout message
   • Wait 15 minutes or use different IP

2. PASSWORD REQUIREMENTS
   • Try creating user with weak password → Should be rejected
   • Password needs: upper, lower, number, special char, 8+ chars

3. USER ISOLATION
   • Create two users
   • Add contacts with User A
   • Login as User B → Should NOT see User A's contacts

4. INPUT VALIDATION
   • Try adding contact with invalid email → Should be rejected
   • Try very long inputs → Should be truncated/rejected

5. ENCRYPTED STORAGE
   • Check database directly: smtp_pass should look like:
     "a1b2c3d4:e5f6a7b8:c9d0e1f2..." (encrypted format)


═══════════════════════════════════════════════════════════════════════════════
📋 SECURITY CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

Before going live, verify:

☐ JWT_SECRET is set (128 character hex)
☐ ENCRYPTION_KEY is set (64 character hex)
☐ Keys are backed up securely
☐ ADMIN_PASSWORD meets complexity requirements
☐ NODE_ENV is set to "production"
☐ HTTPS is enabled (automatic on Render)
☐ Backend logs show "SECURE VERSION"
☐ Re-added SMTP accounts (now encrypted)
☐ Re-connected Cloudflare (now encrypted)
☐ Tested brute force protection
☐ Tested user isolation


═══════════════════════════════════════════════════════════════════════════════
🔄 ROLLBACK (If Needed)
═══════════════════════════════════════════════════════════════════════════════

To rollback to non-secure version:

1. Replace server.js with server-v3-complete.js
2. Replace database.js with database-v3-complete.js
3. Remove security.js
4. Remove ENCRYPTION_KEY from environment
5. Re-deploy

Note: Any data encrypted with the secure version will need to be re-entered.


═══════════════════════════════════════════════════════════════════════════════
📊 SECURITY COMPARISON
═══════════════════════════════════════════════════════════════════════════════

                            │ Before (v3.0) │ After (v3.1)
────────────────────────────┼───────────────┼──────────────
SMTP passwords              │ Plain text    │ AES-256 encrypted
Cloudflare tokens           │ Plain text    │ AES-256 encrypted
User data isolation         │ ❌ None       │ ✅ Complete
Ownership verification      │ ❌ None       │ ✅ All actions
Input validation            │ ⚠️ Minimal    │ ✅ Comprehensive
HTML sanitization           │ ❌ None       │ ✅ XSS protected
Brute force protection      │ ❌ None       │ ✅ 5 attempts/15min
Password complexity         │ ❌ None       │ ✅ Enforced
Per-user rate limiting      │ ❌ None       │ ✅ 100/min
Audit logging               │ ❌ None       │ ✅ Comprehensive
JWT expiry                  │ 7 days        │ 24 hours


═══════════════════════════════════════════════════════════════════════════════
🎉 DONE!
═══════════════════════════════════════════════════════════════════════════════

Your Cold Email System is now production-ready with:

• Military-grade encryption (AES-256-GCM)
• Complete user isolation
• Input validation and sanitization
• Brute force protection
• Comprehensive audit logging

You can now safely use real SMTP credentials and API tokens.

Questions? Issues? The system logs detailed audit trails to help debug
any security-related issues.

Stay secure! 🔐
